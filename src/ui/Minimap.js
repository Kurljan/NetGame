// src/ui/Minimap.js
// Small overview minimap (stub — to be implemented in Phase 2)

export class Minimap {
  constructor(sim, canvas) {
    this.sim    = sim;
    this.canvas = canvas;
    // Minimap will be rendered in a future phase
  }
}
